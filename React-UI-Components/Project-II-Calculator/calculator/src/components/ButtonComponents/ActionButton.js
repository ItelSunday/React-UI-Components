import React from 'react';
import './Button.css';

const ActionButton = props => {
    return (
             <div className="action-btn">
                <ActionButton className="clear">clear</ActionButton>
            </div>

           
    );
};

export default ActionButton;